extends layout

block content
  h1= title
  p Welcome to #{title}

  div
    .col-lg-offset-1.col-lg-8
      form.form-horizontal(novalidate role="form" name="form")
        .form-group
          label.col-sm-2.control-label(for="firstName") First Name
          .col-sm-10
            input.form-control(id="firstName" type="text" placeholder="First Name"  value="#{contact.name.first}" required)

        .form-group
          label.col-sm-2.control-label(for="lastName") Last Name
          .col-sm-10
            input.form-control(id="lastName" type="text" placeholder="Last Name" value="#{contact.name.last}" required)

        .form-group
          label.col-sm-2.control-label(for="email") Email
          .col-sm-10
            input.form-control(id="email" type="email" name="eMail" placeholder="Email" value="#{contact.email}" required)

        .form-group
          label.col-sm-2.control-label(for="city") City
          .col-sm-6
            input.form-control(id="city" type="text" placeholder="City" value="#{contact.location.city}")
          label.col-sm-1.control-label(for="state") State
          .col-sm-3
            input.form-control(id="state" type="text" placeholder="State" value="#{contact.location.state}")

        .form-group
          .col-sm-offset-2.col-sm-2
            button.btn.btn-primary(type="submit") Update
          .col-sm-4
            button.btn.btn-warning(type="button") Clear